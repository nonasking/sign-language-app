# KSL 번역 웹앱

React + Vite + Tailwind + Three.js로 구축한 한국 수어(KSL) 텍스트 번역 웹앱.
백엔드 없는 정적 앱, Vercel 배포 대상.

---

## 작업 원칙

- **설계 결정이 필요한 부분은 구현 전에 반드시 먼저 질문한다.**
  예: 컴포넌트 구조 변경, 새로운 의존성 추가, 좌표계 변경, 애니메이션 방식 변경 등
- **큰 변경사항(여러 파일 수정, 아키텍처 변경)은 접근 방식을 먼저 설명하고 확인받은 뒤 시작한다.**
- 작은 버그 수정이나 명확한 단순 작업은 바로 진행 가능하다.

---

## 커밋 규칙

- **Co-authored-by: Claude 또는 Claude 관련 내용을 커밋 메시지에 절대 포함하지 않는다.**
- 커밋 메시지는 **한국어**로 작성한다.
- 형식: `type(scope): 내용`

| type | 의미 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `refactor` | 리팩토링 (기능 변화 없음) |
| `style` | 스타일/포맷 (로직 변화 없음) |
| `chore` | 빌드, 설정, 의존성 |
| `docs` | 문서 |

scope 예시: `avatar`, `animation`, `dict`, `ui`, `hooks`, `utils`

**예시:**
```
feat(avatar): 손 관절 색상 손가락별로 구분
fix(animation): 지문자 폴백 시 빈 프레임 스킵
refactor(hooks): useSignAnimation RAF 로직 분리
```

---

## 코드 스타일

- 컴포넌트는 **함수형 + hooks** 사용. 클래스 컴포넌트 금지.
- 파일명: 컴포넌트는 **PascalCase** (`SignAvatar.jsx`), hooks/utils는 **camelCase** (`useSignAnimation.js`)
- 주석은 **한국어**로 작성한다.
- 불필요한 주석 금지. 코드가 명확히 설명하는 내용은 주석 생략.
- Three.js 씬 초기화는 `useEffect` + `useRef` 패턴 유지.
- 포즈 데이터 좌표계: Origin=손목, +Y=손가락 방향, +X=새끼손가락 방향, +Z=뷰어 방향.

---

## 하네스: KSL 개발

**목표:** KSL 웹앱의 기능 개발, 코드 품질 유지, 수어 데이터 관리를 일관된 워크플로우로 처리

**트리거:** 기능 구현·수정·버그픽스·수어 데이터 업데이트 등 개발 작업 요청 시 `ksl-develop` 스킬을 사용하라. 단순 질문·설명 요청은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-27 | 초기 구성 | 전체 | - |
